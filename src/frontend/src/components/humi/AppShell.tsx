// Re-export Humi AppShell from ./shell (ported 1:1 from
// docs/design-ref/shelfly-bundle/project/shell.jsx).
// Kept at this path so app/[locale]/layout.tsx import remains stable.

export { AppShell } from './shell/AppShell';
